<a class="trigger-popover" data-content="{{ organization }} est implentée à {% for c in organization.cities %}<br/>{{ c.name }}{% endfor %}" data-title="{{ 'organization'|trans }}: {{ organization }}" href="{{ path('organization', {'id' : organization.id, 'slug' : organization.slug}) }}">{{ organization }}</a>